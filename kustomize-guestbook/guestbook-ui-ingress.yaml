apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: guestbook-ui
  annotations:
    ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - http:
      paths:
        - path: /guestbook
          backend:
            serviceName: guestbook-ui
            servicePort: 80
